const int a = 9;
void setup() {
  pinMode(a, OUTPUT);

}

void loop() {
  for (int dutyCycle = 0; dutyCycle <= 255; dutyCycle +=1) {
    analogWrite(a, dutyCycle);
    delay(5);
  }
  for (int dutyCycle = 255;
  dutyCycle >= 0; dutyCycle -= 1){
    analogWrite(a, dutyCycle);
    delay(5);
  }

}
